- name: jscfg
  watch:
    - "**/*.js"
  on_change: "echo js changed {path}"
  notify: auto
- name: csscfg
  watch:
    - "**/*.css"
  on_change: "echo css changed {path}"
  notify: inject-css
  ignore:
    - "target/**"
